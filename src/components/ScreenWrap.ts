import styled from '@emotion/native';
import { Colors } from '../values/colors';

export const ScreenWrap = styled.View`
  flex: 1;
  background-color: ${Colors.theme};
  align-items: center;
  justify-content: center;
  padding-top: 30px;
  padding-bottom: 0;
`;
